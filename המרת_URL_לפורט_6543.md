# 🔄 המרת URL לפורט 6543 - מדריך מהיר

אם יש לך URL עם פורט 5432, הנה איך להמיר אותו ל-6543 (Connection Pooling).

## אם יש לך Supabase

### שלב 1: קחי את ה-URL הקיים

ה-URL שלך כנראה נראה כך:
```
postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
```

### שלב 2: המרה ידנית

**החלפי את ה-URL כך:**

1. **החלפי את הפורט:**
   - מ: `:5432`
   - ל: `:6543`

2. **החלפי את ה-host:**
   - מ: `db.[PROJECT-REF].supabase.co`
   - ל: `aws-0-[REGION].pooler.supabase.com`

3. **החלפי את ה-username:**
   - מ: `postgres:`
   - ל: `postgres.[PROJECT-REF]:`

### דוגמה:

**URL ישן:**
```
postgresql://postgres:MyPassword123@db.abcdefghijklmnop.supabase.co:5432/postgres
```

**URL חדש (אם ה-REGION הוא eu-west-1):**
```
postgresql://postgres.abcdefghijklmnop:MyPassword123@aws-0-eu-west-1.pooler.supabase.com:6543/postgres
```

### איך למצוא את ה-REGION?

1. לך ל-Supabase Dashboard → Settings → General
2. חפשי "Region" או "Database Region"
3. זה יכול להיות:
   - `eu-west-1` (אירופה מערב)
   - `us-east-1` (ארה"ב מזרח)
   - `us-west-1` (ארה"ב מערב)
   - `ap-southeast-1` (אסיה)
   - וכו'

### אם את לא יודעת מה ה-REGION

נסי את האפשרויות הבאות (החלפי רק את הסיסמה וה-PROJECT-REF):

**אפשרות 1 - אירופה:**
```
postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-eu-west-1.pooler.supabase.com:6543/postgres
```

**אפשרות 2 - ארה"ב מזרח:**
```
postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

**אפשרות 3 - ארה"ב מערב:**
```
postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-us-west-1.pooler.supabase.com:6543/postgres
```

---

## אם יש לך Vercel Postgres

אם יצרת מסד נתונים ב-Vercel Postgres, ה-URL אמור להיות כבר נכון!

אבל אם הוא עדיין עם פורט 5432:

1. לך ל-Vercel Dashboard → הפרויקט → Storage
2. לחצי על המסד נתונים שיצרת
3. חפשי "Connection String" או "Database URL"
4. ודאי שאת משתמשת ב-URL עם Connection Pooling

---

## פתרון מהיר - שינוי מינימלי

אם את לא רוצה לשנות הרבה, נסי את זה:

קחי את ה-URL הקיים והחלפי רק את הפורט והוסף `?pgbouncer=true`:

**מ:**
```
postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
```

**ל:**
```
postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true
```

(שיניתי `:5432` ל-`:6543` והוספתי `?pgbouncer=true` בסוף)

**אבל:** זה לא תמיד עובד. עדיף להשתמש ב-URL המלא עם `pooler.supabase.com`.

---

## איך לבדוק שהתיקון עובד

1. עדכני את `.env.local` עם ה-URL החדש
2. הרצי:
   ```bash
   npm run db:check
   ```
3. אמור לראות:
   ```
   ✅ Connection Pooling (מומלץ ל-Vercel)
   📍 פורט: 6543
   ✅ חיבור הצליח!
   ```

---

## 🆘 עזרה

אם את עדיין לא מצליחה, שלחי לי:
1. את ה-URL הקיים שלך (בלי הסיסמה!)
2. איזה Region את רואה ב-Supabase → Settings → General

ואני אעזור לך ליצור את ה-URL הנכון!

